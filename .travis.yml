
language: java


services:
  - docker
sudo: false
script: mvn clean compile package


after_script:
  - docker build -t bucinkocloud/springboot .
  - echo "$DOCKER_PASSWORD" | docker login -u bucinkocloud --password-stdin
  - docker push bucinkocloud/tasklist:latest
